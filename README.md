# Emad Galal Portfolio

This is the static GitHub Pages export of the portfolio.

The deployable files live at the repository root. The editable React source is retained under `source/`, while published media is stored in `manus-storage/` so the website does not depend on the original hosting environment.

GitHub Pages should remain configured to publish the `main` branch from the root directory.
